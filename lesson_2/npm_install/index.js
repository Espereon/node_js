const valeraMathPackage = require ('valera_math_package');

const addResult = valeraMathPackage.add(3,9);
const subResult = valeraMathPackage.subtract(9,3);

console.log(addResult);
console.log(subResult);